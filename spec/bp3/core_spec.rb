# frozen_string_literal: true

RSpec.describe Bp3::Core do
  it "has a version number" do
    expect(Bp3::Core::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
